## Usage
```
python test.py --clean_root utterances/clean \
               --testing_root utterances/ \
               --metrics pesq stoi sisdr composite \
               --model_uids noisy baseline
```